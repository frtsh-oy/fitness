// Границы недельного объёма и вердикт по нему.
//
// Все числа — СОПОСТАВИМЫЕ подходы в неделю на группу мышц: только целевые
// доли, односторонние по сторонам (см. weekly.js и спеку). Подставлять сюда
// наше внутреннее число нельзя: ориентиры получены в другой системе счёта, и у
// ягодичных это 12 против 18.
//
// У каждой полосы — обоснование (why) и basis: «источник», если число взято из
// исследования, или своя формулировка, если границу провели мы (универсального
// числа в проверенных источниках нет). Ссылка на источник (sources) есть у
// всех полос, кроме «ноль»: там источников нет намеренно — это определение
// (в программе нет упражнения на группу), а не вывод исследования. Менять
// числа можно, не трогая расчёт, — для этого таблица и вынесена в свой файл.
const bands = [
  {
    max: 0,
    verdict: 'не тренируется целенаправленно',
    basis: 'определение',
    why: 'ни одного упражнения, где эта группа целевая. Лечится добавлением упражнения, а не числом подходов.',
    sources: [],
  },
  {
    max: 4,
    verdict: 'работает, но не оптимально',
    basis: 'источник',
    why: 'нижняя изученная категория — меньше пяти подходов в неделю: рост есть, но не лучший.',
    sources: ['https://pubmed.ncbi.nlm.nih.gov/27433992/'],
  },
  {
    max: 9,
    verdict: 'поддержание и слабый рост',
    basis: 'источник',
    why: 'средняя изученная категория, пять—девять подходов. Около шести хватает, чтобы удержать набранное.',
    sources: [
      'https://pubmed.ncbi.nlm.nih.gov/27433992/',
      'https://rpstrength.com/blogs/articles/training-volume-landmarks-muscle-growth',
    ],
  },
  {
    max: 20,
    verdict: 'рабочий объём',
    basis: 'низ из источника, верх — наша линия',
    // «Дали лучший результат» здесь стояло раньше и было сильнее источника:
    // само трёхуровневое деление, из которого взяты границы 4 / 9 / 10,
    // значимости не достигло. Дословно (Schoenfeld 2017, абстракт): «Outcomes
    // for weekly sets as a three-level categorical variable (<5, 5-9 and 10+
    // per muscle) showed a trend for an effect of weekly sets (P = 0.074)».
    // Значимы в той же работе непрерывная модель (P = 0,002) и сравнение
    // «выше/ниже» внутри исследования (P = 0,03) — на них и опирается «больше
    // объёма в среднем давало больше прироста». Положение самой границы (10)
    // от этого не меняется: категория «10+» взята из источника дословно.
    why: 'десять и больше подходов в неделю — верхняя изученная категория. Больше объёма в среднем давало больше прироста, но само деление на три категории значимости не достигло: это тенденция (P = 0,074). Верхнюю границу двадцать проводим мы: универсального числа в проверенных источниках нет.',
    sources: [
      'https://pubmed.ncbi.nlm.nih.gov/27433992/',
      'https://rpstrength.com/blogs/articles/training-volume-landmarks-muscle-growth',
    ],
  },
  {
    max: Infinity,
    // Полоса называлась «выше изученного» — и это была неправда: верхняя
    // категория источника открытая («10+ per muscle»), двадцать первый подход
    // лежит внутри изученного. Наша тут только сама граница двадцать, про неё
    // название и говорит.
    verdict: 'выше нашей границы',
    basis: 'наша линия',
    // Про индивидуальность говорит не мета-регрессия, а страница ориентиров
    // объёма («MAV isn't a fixed number», «Your personal landmarks may vary
    // significantly») — поэтому она здесь во второй ссылке, а не подразумевается
    // из соседних полос.
    why: 'рост с объёмом продолжается, но отдача затухает. Общего потолка в проверенных источниках нет, ориентиры там названы индивидуальными — поэтому полоса называется «выше нашей границы», а не «перегрузка» и не «выше изученного».',
    sources: [
      'https://sportrxiv.org/index.php/server/preprint/view/460',
      'https://rpstrength.com/blogs/articles/training-volume-landmarks-muscle-growth',
    ],
  },
];

export const LOAD_BANDS = Object.freeze(bands.map(band => Object.freeze(band)));

export function verdictFor(sets) {
  if (typeof sets !== 'number' || Number.isNaN(sets)) {
    throw new Error(`объём должен быть числом, получено: ${sets}`);
  }
  if (sets < 0) {
    throw new Error(`объём не может быть отрицательным: ${sets}`);
  }
  // Полосы отсортированы по возрастанию, поэтому первая подходящая и есть
  // ответ. Последняя с max: Infinity гарантирует, что find всегда что-то найдёт.
  return LOAD_BANDS.find(band => sets <= band.max);
}
