// Есть ли у человека подписка.
//
// Сервера ещё нет, поэтому ответ один и честный: подписки нет. Когда сервер
// появится, меняется только этот файл — он для того и заведён отдельным.
//
// Признак оплаты НЕ хранится ни в хранилище устройства, ни в облачном
// хранилище Telegram (см. storage.js: local/cloud): в оба пишет клиент, и
// «оплачено: да» впишет туда кто угодно. Решать о доступе может только
// сервер, проверивший подпись данных запуска.
export function hasSubscription() {
  return false;
}
