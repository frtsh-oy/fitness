# Карта тела (S2) — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Показать на схеме тела, какие группы мышц и с каким объёмом работают в тренировке `legs-mwf`, и дать перейти от мышцы к её упражнениям.

**Architecture:** Расчёт объёма отделён от рисования: `volume.js` превращает тренировку в подходы по группам и регионам без DOM и без библиотеки, `bodymap.js` строит из этого данные для библиотеки и обрабатывает клик. Библиотека лежит в репозитории, а не грузится с CDN.

**Tech Stack:** Ванильный JS (ES-модули), `body-highlighter` 3.0.2 (MIT, сборка в `vendor/`), Node 24 + `node --test` и `jsdom` для тестов.

**Spec:** `docs/superpowers/specs/2026-09-17-body-map-design.md`

## Global Constraints

- Никакого шага сборки. Файлы репозитория — ровно то, что отдаёт хостинг.
- Ноль рантайм-зависимостей из npm. Библиотека карты лежит в `vendor/` как файл, а не как пакет.
- Приложение обязано работать в обычном браузере без `window.Telegram` и без ошибок в консоли.
- Фирменная палитра: фон `#152d4a`, акцент `#ceef69`.
- Весь текст интерфейса русский, обращение к пользователю на «ты».
- Идентификаторы групп мышц берутся только из `muscles.js`; значения `region` обязаны существовать в словаре библиотеки.
- `volume.js` не знает ни про DOM, ни про библиотеку, ни про конкретную тренировку.
- `bodymap.js` не знает про конкретную тренировку.
- Сейчас 237 тестов, все зелёные. Ни один не должен упасть.

---

### Task 1: Библиотека в репозиторий

**Files:**
- Create: `vendor/body-highlighter.esm.js`
- Create: `vendor/body-highlighter.LICENSE`
- Create: `test/vendor.test.js`

**Interfaces:**
- Consumes: ничего
- Produces: `vendor/body-highlighter.esm.js` с экспортами `createBodyHighlighter` (по умолчанию и именованным), `MuscleType`, `ModelType`, `normalizeMuscle`

- [ ] **Step 1: Достать пакет и положить сборку**

```bash
mkdir -p /tmp/bh && cd /tmp/bh && npm pack body-highlighter@3.0.2 && tar -xzf body-highlighter-3.0.2.tgz
cd /Users/manager/Sites/tg-workout && mkdir -p vendor
cp /tmp/bh/package/dist/body-highlighter.esm.js vendor/body-highlighter.esm.js
cp /tmp/bh/package/LICENSE vendor/body-highlighter.LICENSE
rm -rf /tmp/bh
```

- [ ] **Step 2: Написать падающий тест**

```js
// test/vendor.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import createBodyHighlighter, { MuscleType, ModelType, normalizeMuscle } from '../vendor/body-highlighter.esm.js';

test('библиотека импортируется и отдаёт фабрику', () => {
  assert.equal(typeof createBodyHighlighter, 'function');
});

test('словарь мышц содержит регионы, на которые мы рассчитываем', () => {
  const values = new Set(Object.values(MuscleType));
  for (const region of ['gluteal', 'abductors', 'quadriceps', 'hamstring', 'adductor',
                        'calves', 'abs', 'obliques', 'lower-back', 'upper-back',
                        'trapezius', 'chest', 'front-deltoids', 'back-deltoids',
                        'biceps', 'triceps', 'forearm']) {
    assert.ok(values.has(region), `в библиотеке нет региона ${region}`);
  }
});

test('есть оба вида модели', () => {
  assert.deepEqual([ModelType.ANTERIOR, ModelType.POSTERIOR], ['anterior', 'posterior']);
});

test('normalizeMuscle приводит имя к региону', () => {
  assert.equal(normalizeMuscle('gluteal'), 'gluteal');
});

test('сборка самодостаточна: внутри нет внешних импортов', () => {
  const src = readFileSync(new URL('../vendor/body-highlighter.esm.js', import.meta.url), 'utf8');
  const imports = [...src.matchAll(/(?:^|\n)\s*import[^;]*from\s*['"]([^'"]+)['"]/g)].map(m => m[1]);
  assert.deepEqual(imports, [], `сборка тянет ${imports.join(', ')}`);
});

test('лицензия сохранена', () => {
  const text = readFileSync(new URL('../vendor/body-highlighter.LICENSE', import.meta.url), 'utf8');
  assert.match(text, /MIT License/);
});
```

- [ ] **Step 3: Запустить и убедиться, что тесты проходят**

Run: `npm test`
Expected: PASS. Это единственная задача плана, где тест проходит сразу: она проверяет чужой файл, а не наш код. Если какой-то регион в словаре отсутствует — останови работу и сообщи: дальше вся разметка опирается на этот список.

- [ ] **Step 4: Коммит**

```bash
git add vendor test/vendor.test.js
git commit -m "Библиотека карты тела в репозитории

body-highlighter 3.0.2, сборка ESM 18 КБ, лицензия MIT сохранена.
В репозиторий, а не с CDN: внешний адрес означал бы отказ карты
на плохой сети.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: Правки таксономии мышц

**Files:**
- Modify: `muscles.js`
- Modify: `test/muscles.test.js`

**Interfaces:**
- Consumes: `MuscleType` из `vendor/body-highlighter.esm.js`
- Produces: `MUSCLES` с исправленными значениями `region`; `toRegions(load)` без изменений в сигнатуре; `regionLabel(region) -> string` — русское название региона

- [ ] **Step 1: Написать падающие тесты**

Существующий импорт в начале файла расширить — там уже есть `MUSCLES, isMuscleId, muscleLabel, toRegions`, добавить к ним `regionLabel`:

```js
import { MUSCLES, isMuscleId, muscleLabel, toRegions, regionLabel } from '../muscles.js';
```

Дальше дописать в конец файла:

```js
// добавить в test/muscles.test.js
import { MuscleType } from '../vendor/body-highlighter.esm.js';

test('каждый регион существует в словаре библиотеки карты', () => {
  const known = new Set(Object.values(MuscleType));
  for (const [id, m] of Object.entries(MUSCLES)) {
    assert.ok(known.has(m.region), `${id}: региона ${m.region} нет в библиотеке`);
  }
});

test('приводящие используют единственное число, как в библиотеке', () => {
  assert.equal(MUSCLES.adductors.region, 'adductor');
});

test('средняя и малая ягодичные это отводящие, а не большая ягодичная', () => {
  assert.equal(MUSCLES.glutes_med.region, 'abductors');
  assert.equal(MUSCLES.glutes.region, 'gluteal');
  assert.notEqual(MUSCLES.glutes_med.region, MUSCLES.glutes.region);
});

test('дельты разведены по переднему и заднему виду', () => {
  assert.equal(MUSCLES.delts_front.region, 'front-deltoids');
  assert.equal(MUSCLES.delts_rear.region, 'back-deltoids');
  // боковой дельты у библиотеки нет: отдана переднему виду, где видна шапка плеча
  assert.equal(MUSCLES.delts_side.region, 'front-deltoids');
});

test('регионов стало 17', () => {
  assert.equal(new Set(Object.values(MUSCLES).map(m => m.region)).size, 17);
});

test('toRegions складывает группы, попавшие в один регион', () => {
  const regions = toRegions({ lats: 1, upper_back: 0.5 });
  assert.equal(regions.get('upper-back'), 1.5);
});

test('regionLabel даёт русское название региона, а не английский идентификатор', () => {
  assert.equal(regionLabel('gluteal'), 'Ягодичные');
  assert.equal(regionLabel('abductors'), 'Средняя и малая ягодичные');
  assert.equal(regionLabel('adductor'), 'Приводящие');
});

test('regionLabel перечисляет все группы, попавшие в один регион', () => {
  // upper-back это и широчайшие, и верх спины: человеку надо видеть обе
  assert.match(regionLabel('upper-back'), /Широчайшие/);
  assert.match(regionLabel('upper-back'), /Верх спины/i);
  assert.match(regionLabel('front-deltoids'), /Передняя дельта/);
  assert.match(regionLabel('front-deltoids'), /Средняя дельта/);
});

test('regionLabel бросает на неизвестном регионе', () => {
  assert.throws(() => regionLabel('нет-такого'), /нет-такого/);
});
```

- [ ] **Step 2: Запустить и убедиться, что тесты падают**

Run: `npm test`
Expected: FAIL — `adductors: региона adductors нет в библиотеке`, плюс провалы про дельты и число регионов.

- [ ] **Step 3: Поправить значения region в muscles.js**

Изменить ровно пять строк словаря:

```js
  glutes_med:  { ru: 'Средняя и малая ягодичные',  region: 'abductors' },
  adductors:   { ru: 'Приводящие',                 region: 'adductor' },
  delts_front: { ru: 'Передняя дельта',            region: 'front-deltoids' },
  delts_side:  { ru: 'Средняя дельта',             region: 'front-deltoids' },
  delts_rear:  { ru: 'Задняя дельта',              region: 'back-deltoids' },
```

Обновить комментарий над словарём: регионы теперь не «сверяются позже», а обязаны существовать в словаре библиотеки, и это закреплено тестом. Рядом с `delts_side` оставить пояснение, что боковой дельты у библиотеки нет и это осознанное приближение.

Добавить функцию обратного перевода — карта отдаёт при клике идентификатор региона, а человеку надо показать русское название:

```js
// Регион картинки может покрывать несколько наших групп (upper-back это и
// широчайшие, и верх спины), поэтому перечисляем все — иначе человек решит,
// что клик показал не то.
export function regionLabel(region) {
  const names = Object.values(MUSCLES).filter(m => m.region === region).map(m => m.ru);
  if (names.length === 0) throw new Error(`Неизвестный регион: ${region}`);
  return names.length === 1 ? names[0] : names[0] + ', ' + names.slice(1).map(n => n.toLowerCase()).join(', ');
}
```

- [ ] **Step 4: Запустить тесты**

Run: `npm test`
Expected: PASS, все существующие тесты тоже.

- [ ] **Step 5: Перегенерировать сводку разметки**

Run: `node tools/muscle-report.js`
Expected: `docs/muscle-map.md обновлён`. Подписи групп не менялись, поэтому содержимое сводки должно остаться тем же — проверь `git diff docs/muscle-map.md`, он обязан быть пустым. Если не пустой, разберись почему перед коммитом.

- [ ] **Step 6: Коммит**

```bash
git add muscles.js test/muscles.test.js
git commit -m "Регионы мышц приведены к словарю библиотеки карты

adductors в единственное число, дельты разведены по переднему и
заднему виду, средняя и малая ягодичные переехали в abductors как
анатомически верное место. Тест сверяет каждый регион со словарём
библиотеки, поэтому расхождение больше не пройдёт молча.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: Расчёт объёма

**Files:**
- Create: `volume.js`
- Create: `test/volume.test.js`

**Interfaces:**
- Consumes: `MUSCLES`, `toRegions` из `muscles.js`
- Produces:
  - `setsByGroup(workout, kind) -> Map<groupId, number>`
  - `setsByRegion(workout, kind) -> Map<region, number>`
  - `exerciseEntries(workout, kind) -> [{ name, key, muscles: [region], frequency }]`
  - `PALETTE_STEPS` — число уровней палитры (6)

- [ ] **Step 1: Написать падающие тесты**

```js
// test/volume.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { setsByGroup, setsByRegion, exerciseEntries, PALETTE_STEPS } from '../volume.js';
import legs from '../workouts/legs-mwf.js';

test('силовой объём по группам совпадает с разметкой', () => {
  const s = setsByGroup(legs, 'strength');
  assert.equal(s.get('glutes'), 6);
  assert.equal(s.get('hamstrings'), 3);
  assert.equal(s.get('lower_back'), 3);
  assert.equal(s.get('glutes_med'), 3);
  assert.equal(s.get('upper_back'), 3);
  assert.equal(s.get('triceps'), 3);
  assert.equal(s.get('adductors'), 2);
  assert.equal(s.get('abs'), 1);
  assert.equal(s.get('calves'), undefined);
});

test('разминка считается отдельно и даёт дробные значения', () => {
  const w = setsByGroup(legs, 'warmup');
  assert.equal(w.get('hip_flexors'), 6);
  assert.equal(w.get('abs'), 5.5);
  assert.equal(w.get('delts_rear'), 3);
  assert.equal(w.get('upper_back'), 0.5);
});

test('заминка считается отдельно', () => {
  const c = setsByGroup(legs, 'cooldown');
  assert.equal(c.get('hip_flexors'), 1);
  assert.equal(c.get('lower_back'), 1);
});

test('силовые не смешиваются с разминкой', () => {
  assert.equal(setsByGroup(legs, 'strength').get('hip_flexors'), undefined);
});

test('объём по регионам складывает группы одного региона', () => {
  const r = setsByRegion(legs, 'strength');
  assert.equal(r.get('upper-back'), 5);       // upper_back 3 + lats 2
  assert.equal(r.get('front-deltoids'), 4);   // delts_front 2 + delts_side 2
  assert.equal(r.get('gluteal'), 6);
  assert.equal(r.get('abductors'), 3);        // отдельно от gluteal
});

test('записи для библиотеки: по одной на пару упражнение и группа', () => {
  const rows = exerciseEntries(legs, 'strength').filter(e => e.key === 'rdl');
  assert.equal(rows.length, 3);
  const byRegion = new Map(rows.map(r => [r.muscles[0], r.frequency]));
  assert.equal(byRegion.get('hamstring'), 2);    // load 1 × 2 круга
  assert.equal(byRegion.get('gluteal'), 2);
  assert.equal(byRegion.get('lower-back'), 1);   // load 0.5 × 2 круга
  for (const r of rows) assert.equal(r.muscles.length, 1);
});

test('дробный объём округляется, но задействованная группа не исчезает', () => {
  const rows = exerciseEntries(legs, 'warmup');
  for (const r of rows) {
    assert.ok(Number.isInteger(r.frequency), `${r.name}: frequency не целое`);
    assert.ok(r.frequency >= 1, `${r.name}: задействованная группа получила ${r.frequency}`);
  }
});

test('уровней палитры шесть — по максимуму объёма в тренировке', () => {
  assert.equal(PALETTE_STEPS, 6);
  assert.equal(Math.max(...setsByRegion(legs, 'strength').values()), PALETTE_STEPS);
});

test('неизвестный тип упражнения даёт пустой результат, а не ошибку', () => {
  assert.equal(setsByGroup(legs, 'нет-такого').size, 0);
});
```

- [ ] **Step 2: Запустить и убедиться, что тесты падают**

Run: `npm test`
Expected: FAIL — `Cannot find module '../volume.js'`.

- [ ] **Step 3: Написать volume.js**

```js
// Расчёт тренировочного объёма. Ни DOM, ни библиотеки карты, ни конкретной
// тренировки: на вход объект по схеме, на выход числа. Это же основание для
// будущей оценки недельной нагрузки, поэтому модуль обязан жить отдельно
// от рисования картинки.
import { MUSCLES, toRegions } from './muscles.js';

// Максимум объёма на регион в текущей тренировке — шесть подходов на ягодичные.
// Столько же уровней в палитре, иначе разница между 6 и 3 подходами пропадёт.
export const PALETTE_STEPS = 6;

function itemsOfKind(workout, kind) {
  const rows = [];
  for (const block of workout.blocks) {
    for (const item of block.items) {
      if (item.kind !== kind) continue;
      rows.push({ item, rounds: block.rounds });
    }
  }
  return rows;
}

export function setsByGroup(workout, kind) {
  const sets = new Map();
  for (const { item, rounds } of itemsOfKind(workout, kind)) {
    for (const [group, share] of Object.entries(item.load)) {
      sets.set(group, (sets.get(group) ?? 0) + share * rounds);
    }
  }
  return sets;
}

export function setsByRegion(workout, kind) {
  const sets = new Map();
  for (const [group, value] of setsByGroup(workout, kind)) {
    const region = MUSCLES[group].region;
    sets.set(region, (sets.get(region) ?? 0) + value);
  }
  return sets;
}

export function exerciseEntries(workout, kind) {
  const entries = [];
  for (const { item, rounds } of itemsOfKind(workout, kind)) {
    // toRegions сворачивает группы упражнения в регионы, поэтому две группы
    // одного региона внутри одного упражнения дают одну запись, а не две.
    for (const [region, share] of toRegions(item.load)) {
      // Округление вверх от нуля: доля 0.5 на один круг — это половина
      // подхода, но группа задействована, и серой на карте быть не должна.
      entries.push({
        name: item.name,
        key: item.key,
        muscles: [region],
        frequency: Math.max(1, Math.round(share * rounds)),
      });
    }
  }
  return entries;
}
```

- [ ] **Step 4: Запустить тесты**

Run: `npm test`
Expected: PASS.

- [ ] **Step 5: Сверить с сводкой разметки**

Run: `node -e "import('./volume.js').then(async v=>{const l=(await import('./workouts/legs-mwf.js')).default;const m=await import('./muscles.js');console.log([...v.setsByGroup(l,'strength')].sort((a,b)=>b[1]-a[1]).map(([g,n])=>m.muscleLabel(g)+' '+n).join('\n'))})"`
Expected: список совпадает с таблицей «Силовые подходы за одну тренировку» в `docs/muscle-map.md` построчно. Если расходится — расчёт неверен, разберись до коммита.

- [ ] **Step 6: Коммит**

```bash
git add volume.js test/volume.test.js
git commit -m "Расчёт тренировочного объёма

setsByGroup и setsByRegion считают подходы как долю нагрузки на число
кругов, раздельно по типу упражнения. exerciseEntries готовит записи
для библиотеки карты: по одной на пару упражнение и регион.

Модуль без DOM и без библиотеки: это же основание для будущей оценки
недельной нагрузки.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: Слой над библиотекой

**Files:**
- Create: `bodymap.js`
- Create: `test/bodymap.test.js`

**Interfaces:**
- Consumes: `exerciseEntries`, `setsByGroup`, `PALETTE_STEPS` из `volume.js`; `MUSCLES`, `regionLabel` из `muscles.js`; `createBodyHighlighter`, `ModelType` из `vendor/body-highlighter.esm.js`
- Produces: `createBodyMap({ workout, anteriorHost, posteriorHost, onPick }) -> { destroy }`, где `onPick({ region, label, sets, exercises })`; `PALETTE`; `warmupOnlyGroups(workout) -> string[]`; `idleGroups(workout) -> string[]`

- [ ] **Step 1: Написать падающие тесты**

```js
// test/bodymap.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { makeDom } from './setup.js';
import { createBodyMap, PALETTE, warmupOnlyGroups, idleGroups } from '../bodymap.js';
import { PALETTE_STEPS } from '../volume.js';
import legs from '../workouts/legs-mwf.js';

function hosts() {
  const { document } = makeDom('<!doctype html><html><body><div id="a"></div><div id="b"></div></body></html>');
  return { document, a: document.getElementById('a'), b: document.getElementById('b') };
}

test('палитра ровно на число уровней объёма', () => {
  assert.equal(PALETTE.length, PALETTE_STEPS);
  for (const color of PALETTE) assert.match(color, /^#[0-9a-f]{6}$/i);
});

test('рисуются оба вида', () => {
  const { a, b } = hosts();
  createBodyMap({ workout: legs, anteriorHost: a, posteriorHost: b });
  assert.ok(a.querySelector('svg'), 'передний вид не отрисован');
  assert.ok(b.querySelector('svg'), 'задний вид не отрисован');
});

test('группы, которые греются только в разминке, перечислены отдельно', () => {
  const only = warmupOnlyGroups(legs);
  assert.ok(only.includes('hip_flexors'), 'сгибатели бедра грузятся только в разминке');
  assert.ok(only.includes('delts_rear'), 'задняя дельта грузится только в разминке');
  assert.ok(!only.includes('glutes'), 'ягодичные есть и в силовых');
});

test('незадействованные группы перечислены отдельно', () => {
  assert.deepEqual(idleGroups(legs).sort(), ['calves', 'forearms']);
});

test('клик по мышце отдаёт её упражнения', () => {
  const { a, b } = hosts();
  const picks = [];
  createBodyMap({ workout: legs, anteriorHost: a, posteriorHost: b, onPick: p => picks.push(p) });
  const target = a.querySelector('[data-muscle="gluteal"]') ?? a.querySelector('polygon');
  assert.ok(target, 'в отрисованном виде нет кликабельных элементов');
  target.dispatchEvent(new a.ownerDocument.defaultView.MouseEvent('click', { bubbles: true }));
  assert.equal(picks.length, 1, 'обработчик клика не вызвался');
  assert.ok(Array.isArray(picks[0].exercises));
  assert.ok(picks[0].exercises.length > 0, 'список упражнений пуст');
  assert.match(picks[0].label, /[А-Яа-я]/, 'название региона не по-русски');
});

test('destroy убирает разметку и не бросает при повторном вызове', () => {
  const { a, b } = hosts();
  const map = createBodyMap({ workout: legs, anteriorHost: a, posteriorHost: b });
  map.destroy();
  assert.equal(a.querySelector('svg'), null);
  assert.doesNotThrow(() => map.destroy());
});
```

- [ ] **Step 2: Запустить и убедиться, что тесты падают**

Run: `npm test`
Expected: FAIL — `Cannot find module '../bodymap.js'`.

Если после реализации тест про клик не сможет найти кликабельный элемент, потому что библиотека под jsdom рисует иначе, — не подменяй библиотеку подделкой молча. Разберись, что именно она рисует (`console.log(a.innerHTML.slice(0, 500))`), и подправь селектор в тесте под фактическую разметку. Подделка допустима только если библиотека под jsdom не работает вовсе; тогда напиши об этом в отчёте и проверь клик вживую в браузере на шаге Task 5.

- [ ] **Step 3: Написать bodymap.js**

```js
// Слой между расчётом объёма и чужой библиотекой. Не знает ни про конкретную
// тренировку, ни про разметку страницы: получает объект тренировки и два
// контейнера.
import createBodyHighlighter, { ModelType } from './vendor/body-highlighter.esm.js';
import { exerciseEntries, setsByGroup, PALETTE_STEPS } from './volume.js';
import { MUSCLES, regionLabel } from './muscles.js';

// Шесть оттенков от бледного к насыщенному. Библиотека выбирает цвет как
// PALETTE[min(len-1, подходы-1)], поэтому порядок здесь и есть шкала.
export const PALETTE = ['#eaf6cb', '#ddf0a8', '#d2ec86', '#c6e765', '#a9d248', '#8ab82f'];

// Мышцы без силовой нагрузки: заметно светлее фона секции, но не белые —
// они есть на схеме, просто не работают.
const IDLE_COLOR = '#cdd8e5';

export function warmupOnlyGroups(workout) {
  const strength = setsByGroup(workout, 'strength');
  const soft = new Map([...setsByGroup(workout, 'warmup'), ...setsByGroup(workout, 'cooldown')]);
  return [...soft.keys()].filter(group => !strength.has(group));
}

export function idleGroups(workout) {
  const touched = new Set();
  for (const kind of ['strength', 'warmup', 'cooldown']) {
    for (const group of setsByGroup(workout, kind).keys()) touched.add(group);
  }
  return Object.keys(MUSCLES).filter(group => !touched.has(group));
}

export function createBodyMap({ workout, anteriorHost, posteriorHost, onPick }) {
  const data = exerciseEntries(workout, 'strength');

  const handle = ({ muscle, data: stats }) => {
    if (!onPick) return;
    // Наружу отдаём русское название, а не идентификатор региона: вызывающий
    // код показывает это человеку, и «gluteal» его только запутает.
    onPick({
      region: muscle,
      label: regionLabel(muscle),
      sets: stats?.frequency ?? 0,
      exercises: stats?.exercises ?? [],
    });
  };

  const common = {
    data,
    bodyColor: IDLE_COLOR,
    highlightedColors: PALETTE.slice(0, PALETTE_STEPS),
    onClick: handle,
    style: { width: '100%' },
  };

  const views = [
    createBodyHighlighter({ ...common, container: anteriorHost, type: ModelType.ANTERIOR }),
    createBodyHighlighter({ ...common, container: posteriorHost, type: ModelType.POSTERIOR }),
  ];

  let alive = true;
  return {
    destroy() {
      if (!alive) return;          // повторный вызов не должен бросать
      alive = false;
      for (const view of views) view.destroy();
    },
  };
}
```

- [ ] **Step 4: Запустить тесты**

Run: `npm test`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add bodymap.js test/bodymap.test.js
git commit -m "Слой над библиотекой карты тела

Строит записи из расчёта объёма, создаёт передний и задний вид,
переводит клик библиотеки в понятный обработчик. Отдельно считает
группы, которые греются только в разминке, и те, что не задействованы
вовсе — на карте они останутся светлыми, и это надо подписать.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: Секция на странице

**Files:**
- Modify: `index.html`
- Modify: `style.css`
- Modify: `app.js`
- Create: `test/bodymap-section.test.js`

**Interfaces:**
- Consumes: `createBodyMap`, `warmupOnlyGroups`, `idleGroups` из `bodymap.js`; `muscleLabel` из `muscles.js`
- Produces: ничего для последующих задач

- [ ] **Step 1: Написать падающие тесты**

```js
// test/bodymap-section.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import { makeDom } from './setup.js';
import { startApp } from '../app.js';

const html = readFileSync(new URL('../index.html', import.meta.url), 'utf8');

function mount() {
  const { window } = makeDom(html);
  return { window, app: startApp(window) };
}

test('секция карты есть в разметке и свёрнута по умолчанию', () => {
  const { window } = mount();
  const section = window.document.querySelector('.bodymap');
  assert.ok(section, 'секции .bodymap нет');
  assert.equal(section.querySelector('.bodymap-toggle').getAttribute('aria-expanded'), 'false');
  assert.equal(section.querySelector('.bodymap-body').hidden, true);
});

test('клик по заголовку раскрывает карту и рисует оба вида', () => {
  const { window } = mount();
  const d = window.document;
  d.querySelector('.bodymap-toggle').click();
  assert.equal(d.querySelector('.bodymap-toggle').getAttribute('aria-expanded'), 'true');
  assert.equal(d.querySelector('.bodymap-body').hidden, false);
  assert.ok(d.querySelector('.bodymap-anterior svg'), 'передний вид не отрисован');
  assert.ok(d.querySelector('.bodymap-posterior svg'), 'задний вид не отрисован');
});

test('повторный клик сворачивает', () => {
  const { window } = mount();
  const t = window.document.querySelector('.bodymap-toggle');
  t.click(); t.click();
  assert.equal(t.getAttribute('aria-expanded'), 'false');
  assert.equal(window.document.querySelector('.bodymap-body').hidden, true);
});

test('состояние раскрытия помнится между открытиями приложения', () => {
  const { window } = mount();
  window.document.querySelector('.bodymap-toggle').click();
  const saved = Object.keys(window.localStorage).filter(k => k.includes('bodymap'));
  assert.equal(saved.length, 1, 'состояние секции не сохранено');
  startApp(window);
  assert.equal(window.document.querySelector('.bodymap-toggle').getAttribute('aria-expanded'), 'true');
});

test('подписи про разминку и незадействованные группы на месте', () => {
  const { window } = mount();
  window.document.querySelector('.bodymap-toggle').click();
  const note = window.document.querySelector('.bodymap-note').textContent;
  assert.match(note, /Сгибатели бедра/, 'не сказано, что греется только в разминке');
  const idle = window.document.querySelector('.bodymap-idle').textContent;
  assert.match(idle, /Икроножные/);
  assert.match(idle, /Предплечья/);
});

test('карта рисуется только при первом раскрытии, а не при загрузке', () => {
  const { window } = mount();
  assert.equal(window.document.querySelector('.bodymap-anterior svg'), null,
    'карта отрисована до раскрытия');
});
```

- [ ] **Step 2: Запустить и убедиться, что тесты падают**

Run: `npm test`
Expected: FAIL — `секции .bodymap нет`.

- [ ] **Step 3: Добавить разметку в index.html**

Вставить после `<nav class="block-nav">` и до `<div class="progress-row">`:

```html
<section class="bodymap">
<button class="bodymap-toggle" type="button" aria-expanded="false" aria-controls="bodymap-body">Что работает в этой тренировке</button>
<div class="bodymap-body" id="bodymap-body" hidden>
<div class="bodymap-views"><div class="bodymap-anterior"></div><div class="bodymap-posterior"></div></div>
<p class="bodymap-note"></p>
<p class="bodymap-idle"></p>
<div class="bodymap-pick" aria-live="polite"></div>
</div>
</section>
```

- [ ] **Step 4: Добавить стили в style.css**

Дописать в конец файла:

```css
.bodymap{margin:18px 0}
.bodymap-toggle{width:100%;text-align:left;border:1px solid #cad6e4;background:white;border-radius:10px;
  font:inherit;font-size:15px;font-weight:650;color:#193d6a;min-height:44px;padding:11px 14px;cursor:pointer}
.bodymap-toggle[aria-expanded=true]{border-color:#7590b2;background:#e4edf8}
.bodymap-body{border:1px solid #d2dce8;border-top:0;border-radius:0 0 10px 10px;padding:14px}
.bodymap-views{display:flex;gap:14px;justify-content:center}
.bodymap-views>div{flex:1;min-width:0;max-width:220px}
.bodymap-note,.bodymap-idle{font-size:13px;line-height:1.5;color:#526278;margin:10px 0 0}
.bodymap-pick:not(:empty){margin-top:12px;padding-top:12px;border-top:1px solid #e2e9f2}
.bodymap-pick h3{font-size:15px;margin:0 0 6px}
.bodymap-pick button{display:block;width:100%;text-align:left;border:0;background:none;font:inherit;
  font-size:14px;color:#193d6a;text-decoration:underline;text-underline-offset:3px;
  min-height:44px;padding:6px 0;cursor:pointer}
@media (max-width:520px){.bodymap-views{flex-direction:column;align-items:center}
  .bodymap-views>div{max-width:260px;width:100%}}
```

- [ ] **Step 5: Связать в app.js**

Добавить импорты рядом с существующими:

```js
import { createBodyMap, warmupOnlyGroups, idleGroups } from './bodymap.js';
import { muscleLabel } from './muscles.js';
```

Внутри `startApp`, после того как разметка тренировки уже построена, добавить:

```js
  // Карта тела. Отложено именно РИСОВАНИЕ: сам модуль библиотеки грузится
  // вместе с приложением, статическим импортом. Отложена работа по построению
  // двух SVG-схем, которая большинству открытий не нужна.
  const mapToggle = document.querySelector('.bodymap-toggle');
  const mapBody = document.querySelector('.bodymap-body');
  const mapPick = document.querySelector('.bodymap-pick');
  const MAP_KEY = 'bodymap-open';
  let bodyMap = null;

  const listGroups = groups => groups.map(muscleLabel).join(', ');

  function fillNotes() {
    const warm = warmupOnlyGroups(workout);
    document.querySelector('.bodymap-note').textContent = warm.length
      ? `Цветом показаны силовые подходы. Только в разминке и заминке работают: ${listGroups(warm)}.`
      : 'Цветом показаны силовые подходы.';
    const idle = idleGroups(workout);
    document.querySelector('.bodymap-idle').textContent = idle.length
      ? `Светлым — то, что эта тренировка не затрагивает: ${listGroups(idle)}.`
      : '';
  }

  function showPick({ label, sets, exercises }) {
    mapPick.replaceChildren();
    const title = document.createElement('h3');
    title.textContent = `${label} — ${sets} подх.`;
    mapPick.append(title);
    for (const name of exercises) {
      const link = document.createElement('button');
      link.type = 'button';
      link.textContent = name;
      link.addEventListener('click', () => {
        const target = [...host.querySelectorAll('.exercise h3')].find(h => h.textContent === name);
        target?.scrollIntoView({ block: 'center',
          behavior: win.matchMedia('(prefers-reduced-motion: reduce)').matches ? 'instant' : 'smooth' });
      });
      mapPick.append(link);
    }
  }

  function openMap() {
    if (!bodyMap) {
      bodyMap = createBodyMap({
        workout,
        anteriorHost: document.querySelector('.bodymap-anterior'),
        posteriorHost: document.querySelector('.bodymap-posterior'),
        onPick: showPick,
      });
      fillNotes();
    }
  }

  function setMapOpen(open) {
    mapToggle.setAttribute('aria-expanded', String(open));
    mapBody.hidden = !open;
    if (open) openMap();
    try { win.localStorage?.setItem(MAP_KEY, open ? '1' : '0'); } catch { /* приватный режим */ }
  }

  mapToggle.addEventListener('click', () =>
    setMapOpen(mapToggle.getAttribute('aria-expanded') !== 'true'));

  let mapWasOpen = false;
  try { mapWasOpen = win.localStorage?.getItem(MAP_KEY) === '1'; } catch { /* см. выше */ }
  if (mapWasOpen) setMapOpen(true);
```

Имена сверены с текущим `app.js`: `startApp(win)` на строке 62, `const document = win.document` на 63, `const workout` на 65, `const host` на 73. Код выше опирается на них и вставляется внутрь `startApp` после построения разметки тренировки.

- [ ] **Step 6: Запустить тесты**

Run: `npm test`
Expected: PASS, все существующие тоже.

- [ ] **Step 7: Проверить в браузере**

Run: `python3 -m http.server 8000`

Открыть страницу инструментами браузера и убедиться:
- строка «Что работает в этой тренировке» стоит после навигации по блокам и до счётчика;
- до клика карты нет в DOM;
- клик раскрывает, видны оба вида, ягодичные окрашены заметно насыщеннее пресса;
- клик по ягодичным даёт список упражнений, клик по упражнению прокручивает к нему;
- подписи про разминку и незадействованные группы читаются;
- повторный клик сворачивает, перезагрузка помнит состояние;
- в консоли нет ошибок.

Отдельно на вьюпорте 375×812: виды становятся друг под другом, горизонтальной прокрутки нет, кнопки не меньше 44 пикселей.

Сервер останови за собой. Приведи в отчёте, что именно проверил.

- [ ] **Step 8: Коммит**

```bash
git add index.html style.css app.js test/bodymap-section.test.js
git commit -m "Секция карты тела на странице

Свёрнутая строка после навигации по блокам; карта рисуется при первом
раскрытии, а не при загрузке. Клик по мышце показывает её упражнения
с числом подходов и прокручивает к упражнению на странице. Состояние
раскрытия помнится в localStorage — это удобство устройства, а не
данные тренировки.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: Публикация

**Files:**
- Modify: `README.md`
- Verify: `tools/collect-site.mjs` (изменять не требуется)

**Interfaces:**
- Consumes: всё предыдущее
- Produces: дерево, готовое к выкладке

- [ ] **Step 1: Проверить, что сборщик подхватил новые файлы**

Run: `node tools/collect-site.mjs`
Expected: файлов стало больше 11 — добавились `bodymap.js`, `volume.js`, `muscles.js` и `vendor/body-highlighter.esm.js`. Сборщик идёт по графу импортов, поэтому правок в нём не нужно; если какой-то из этих файлов в списке не появился, значит он не импортируется со страницы — разберись.

Отдельно проверь, что в `dist/` НЕ попали `test`, `src-original`, `docs`, `tools`, `workouts/schema.js` (если он не импортируется страницей) и `vendor/body-highlighter.LICENSE`.

Про лицензию: файл лицензии не нужен браузеру, но MIT требует сохранять текст лицензии при распространении. Он остаётся в репозитории рядом со сборкой, и этого достаточно. В отчёте отметь, что проверил это осознанно.

- [ ] **Step 2: Проверить собранную папку локально**

```bash
cd dist && python3 -m http.server 8010
```

Открыть и убедиться, что карта раскрывается и работает из собранной папки, а не только из корня репозитория. Сервер останови.

- [ ] **Step 3: Дописать README**

В раздел «Структура» добавить строки:

```
| `volume.js` | расчёт подходов по группам и регионам мышц |
| `bodymap.js` | карта тела: слой над библиотекой body-highlighter |
| `vendor/` | сборка body-highlighter 3.0.2 (MIT) и её лицензия |
```

Добавить раздел после «Разметка мышц и её вычитка»:

```markdown
## Карта тела

Секция «Что работает в этой тренировке» показывает на схеме, какие группы
мышц работают, и переводит от мышцы к её упражнениям. Цвет означает число
рабочих подходов за тренировку: доля нагрузки упражнения, умноженная на
число кругов блока. Считаются только силовые упражнения — разминка и
заминка перечислены подписью под схемой.

Схема рисует регионы библиотеки, а не наши группы, поэтому три пучка дельт
показываются двумя: передним и задним. Клик по дельте вернёт упражнения
всех пучков.

Библиотека лежит в `vendor/`, а не грузится с CDN: внешний адрес означал бы,
что на плохой сети карта не откроется.
```

- [ ] **Step 4: Прогнать тесты целиком**

Run: `npm test`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add README.md
git commit -m "README: карта тела и новые модули

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

- [ ] **Step 6: Остановиться и передать управление**

Выкладка на GitHub Pages происходит отправкой в `main`, и это делает контролёр после ревью. Ничего не публикуй сам.
